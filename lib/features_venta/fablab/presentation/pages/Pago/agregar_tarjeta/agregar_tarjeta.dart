import 'package:flutter/material.dart';

class AgregarTarjeta extends StatelessWidget {
  const AgregarTarjeta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
